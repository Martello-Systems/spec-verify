// A Next.js App Router page for /about only. There is NO /widgets route here,
// so route-exists path="/widgets" must report FAIL (no over-matching).
export default function AboutPage() {
  return null;
}
