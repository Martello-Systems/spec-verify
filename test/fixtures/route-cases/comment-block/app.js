import express from 'express';

const app = express();

/*
 * Planned, not implemented:
 *   app.post('/widgets', createWidget)
 * route-exists must report FAIL: this route lives only inside a block comment.
 */

export { app };
